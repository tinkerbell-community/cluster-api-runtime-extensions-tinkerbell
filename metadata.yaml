# clusterctl RuntimeExtensionProvider metadata for
# cluster-api-runtime-extensions-tinkerbell. The contract is the CAPI API
# contract of the pinned sigs.k8s.io/cluster-api module — verified against
# go.mod by `make verify-contract` so it can never drift into CAREN's
# stamped-literal bug (spec §2.7): cluster-api v1.13.0 -> contract v1beta2.
apiVersion: clusterctl.cluster.x-k8s.io/v1alpha3
releaseSeries:
  - major: 0
    minor: 1
    contract: v1beta2
